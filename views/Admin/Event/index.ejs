<div class="container">
  <div class="row">
    <div class="col-lg-12">
      <h1 class="page-header"><%= __('events') %> <a href="/admin/event/add" class="btn btn-danger pull-right"><i class="fa fa-plus"></i> <%= __('add_new_event') %></a></h1>
    </div>
    <div class="row">
      <div class="panel ">
        <div class="panel-body">
          <%- partial ('../../partials/pagination.ejs') %>
          <table class="table">
            <tbody>
              <% for(i in events) { %>
              <tr>
                <td><img src="<%- events[i].thumb %>" alt="" width="75"/></td>
                <td><%- events[i].name %></td>
                <td>
                  <a class="btn btn-danger pull-right" href="/admin/event/edit/<%- events[i].id %>"><%= __('edit') %></a>
                </td>
              </tr>
              <% } %>
            </tbody>
          </table>
          <%- partial ('../../partials/pagination.ejs') %>
        </div>
      </div>
    </div>
  </div>
</div>
